# Claude Code Prompt — Implement from Figma handoff

Use `/docs/design-handoff/*` as source of truth.
Implement screens and flows in small iterations.
Do not rebuild architecture.
Use SwiftUI + SwiftData + Clean MVVM.
RU-first labels, currency only KZT (₸).

Per iteration:
1) Screen mapping (Figma -> View -> VM -> UseCase)
2) Implement UI parity + interactions
3) Add/update tests where logic changed
4) Run regression checks and summarize changed files
